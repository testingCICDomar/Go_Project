version: '3'
services:
  master:
    build: ./jenkins-master
    ports:
      - "50000:50000"
      - "8080:8080"
    volumes:
      - jenkins-log:/var/log/jenkins
      - jenkins-data:/var/jenkins_home
    networks:
      - App-net

volumes:
  jenkins-log:
  jenkins-data:


networks:
  App-net:
